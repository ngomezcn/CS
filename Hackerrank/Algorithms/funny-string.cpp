#include <iostream>
#include <vector>
#include <bits/stdc++.h>

using namespace std;

int main()
{
    vector<int> arr;
    for (size_t i = 0; i < arr.size(); i++)
    {

        if (arr[i] == v)
        {
            return i;
        }
    }
}